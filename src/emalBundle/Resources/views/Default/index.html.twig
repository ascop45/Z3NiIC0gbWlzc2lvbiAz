{# Page principale #}
{{ form_start(form) }}

    {# Erreur du formulaire#}
    {{ form_errors(form) }}
    
    {# Login #}
    {{ form_label(form.identifiant, "Identifiant : ") }}
    {{ form_widget(form.identifiant) }}
    
    {# Mot de passe #}
    {{ form_label(form.motDePasse, "Mot de passe : ") }}
    {{ form_widget(form.motDePasse) }}
    
    {# Type de profil #}
    {{ form_label(form.profil, "Type de profil : ") }}
    {{ form_widget(form.profil) }}
    
    {# Soumission #}
    {{ form_widget(form.valider) }}

{{ form_end(form) }}